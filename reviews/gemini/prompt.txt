You are reviewing a Bayesian laboratory QC prototype (FastAPI + Vue/ECharts).

Review ONLY the provided git diff.

Focus: do these changes produce valid, non-misleading, actionable signals for lab personnel (charting, Bayesian outputs, alert linkage)?

Deliverables:
1) P0/P1/P2 findings with concrete risk (correctness, actionability, UX trust).
2) Call out any math/stat correctness issues (Bayesian state, prior boundaries, out-of-order ingestion, recompute logic).
3) Call out API/UI alignment issues and potential regressions.
4) Suggest fixes with file references.

Be concise but specific.
